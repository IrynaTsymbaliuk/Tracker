package com.tracker.core.types

/**
 * Available metrics that can be tracked by LifeMetrics.
 * Initially only LANGUAGE_LEARNING is supported.
 */
enum class Metric {
    LANGUAGE_LEARNING
    // Future: WORKOUT, SLEEP, STEPS, WEIGHT, READING, WATCHING
}
